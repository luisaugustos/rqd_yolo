name: rqd-ai-lab
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - cudatoolkit=11.8
  - pytorch>=2.3
  - torchvision>=0.18
  - pip:
      - -e .[dev,notebooks]
